# Legacy Placeholder: `backend/strapi-content-types`

Эта директория не участвует в текущем runtime проекта.

Текущее архитектурное решение:
- source of truth для content types находится внутри `backend/strapi-app`;
- отдельный пакет `strapi-content-types` не используется;
- новые изменения схем Strapi нужно вносить непосредственно в `backend/strapi-app`.

Директория сохранена только как исторический маркер, чтобы не создавать ложного ощущения, что здесь есть активный модуль.
