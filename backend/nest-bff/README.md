# Legacy Placeholder: `backend/nest-bff`

Эта директория не участвует в текущем runtime проекта.

Текущее архитектурное решение:
- единственный backend находится в `backend/strapi-app`;
- отдельный `nest-bff` не используется;
- новые production и development сценарии не должны зависеть от содержимого этой директории.

Если в будущем понадобится отдельный BFF-слой, его нужно проектировать заново под актуальный scope проекта, а не считать эту директорию рабочим модулем по умолчанию.
